apiVersion: v1
kind: Service
metadata:
  labels:
    app: notification-mongodb
  name: notification-mongodb
spec:
  ports:
  - name: "27017"
    port: 27017
    targetPort: 27017
  selector:
    app: notification-mongodb